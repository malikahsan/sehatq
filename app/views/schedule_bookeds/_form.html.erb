<%= form_with(model: schedule_booked, local: true) do |form| %>
  <% if schedule_booked.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule_booked.errors.count, "error") %> prohibited this schedule_booked from being saved:</h2>

      <ul>
      <% schedule_booked.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :patient_name %>
    <%= form.text_field :patient_name %>
  </div>

  <div class="field">
    <%= form.label :schedule_id %>
    <%= collection_select(:schedule_booked, :schedule_id, Schedule.all, :id, :schedule_date, prompt: true) %>
    <%#= form.text_field :schedule_id %>
  </div>

  <div class="field">
    <%= form.label :doctor_id %>
    <%= collection_select(:schedule_booked, :doctor_id, Doctor.all, :id, :name, prompt: true) %>
    <%#= form.text_field :doctor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
